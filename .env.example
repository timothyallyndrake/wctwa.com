# Public (Astro) — baked in at build time
PUBLIC_API_URL=https://api.example.com

# Lambda / local API testing
LEAD_NOTIFY_TO=you@example.com,master@example.com
LEAD_NOTIFY_FROM=noreply@wctwa.com
AWS_REGION=us-west-2

# Optional: allow CORS origin in local/dev
CORS_ORIGIN=http://localhost:4321
